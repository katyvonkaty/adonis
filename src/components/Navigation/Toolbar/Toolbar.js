import React from "react"
import Logo from "../../Logo/Logo"

const Toolbar = (props) => {
  return(
    <>
    <header className="toolbar">
      <div>MENU</div>
      <div> <Logo /> </div>
      <nav>
        ...
      </nav>
    </header>
    </>
  )
}

export default Toolbar
